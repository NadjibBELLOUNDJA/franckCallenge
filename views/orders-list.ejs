<!DOCTYPE html>

<html>
	<head>
		<meta charset="utf-8">
		<title> Orders List </title>
	</head>
	<body>
		<h1> Orders List </h1>

		<div>
			<ul>
				<% orderslist.forEach(function(order, index) { %>
					<li> 
						Commande: <%= order.name %><br/>
						Numero: <%= order.orderNumber %><br/>
						Double: <%= order.double %><br/>
						Livraison prévue à: <%= order.deliveryHour %> <br/>
						Etat: <%= order.status %> <br/>
						<a href="/orderslist/finish/<%= index %>"> (Mark as finished) </a>
					</li>
				<% });%>
			</ul>
		</div>
	</body>
</html>